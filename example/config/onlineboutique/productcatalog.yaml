version: v1.0.5
name: productcatalog
adapters:
  - server:
      action: simpleRpc
      variant: grpc
      route: ListProducts
    steps: []
  - server:
      action: simpleRpc
      variant: grpc
      route: GetProduct
    steps: []
